apiVersion: v1
kind: ConfigMap
metadata:
  name: atc-capital-config
data:
  DJANGO_ALLOWED_HOSTS: "api.staging.atc-capital.com"
  CORS_ALLOWED_ORIGINS: "https://app.staging.atc-capital.com"
  CSRF_TRUSTED_ORIGINS: "https://app.staging.atc-capital.com"
  DEPLOYMENT_ENVIRONMENT: "staging"
  APP_VERSION: "staging"