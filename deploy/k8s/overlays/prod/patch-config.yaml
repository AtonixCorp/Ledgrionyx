apiVersion: v1
kind: ConfigMap
metadata:
  name: atc-capital-config
data:
  DJANGO_ALLOWED_HOSTS: "api.atc-capital.com"
  CORS_ALLOWED_ORIGINS: "https://app.atc-capital.com"
  CSRF_TRUSTED_ORIGINS: "https://app.atc-capital.com"
  DEPLOYMENT_ENVIRONMENT: "prod"
  APP_VERSION: "prod"