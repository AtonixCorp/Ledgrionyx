apiVersion: v1
kind: ConfigMap
metadata:
  name: ledgrionyx-config
data:
  DJANGO_ALLOWED_HOSTS: "api.dev.ledgrionyx.com,localhost"
  CORS_ALLOWED_ORIGINS: "https://app.dev.ledgrionyx.com"
  CSRF_TRUSTED_ORIGINS: "https://app.dev.ledgrionyx.com"
  DEPLOYMENT_ENVIRONMENT: "dev"
  APP_VERSION: "dev"