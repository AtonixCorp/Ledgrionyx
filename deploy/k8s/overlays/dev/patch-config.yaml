apiVersion: v1
kind: ConfigMap
metadata:
  name: atc-capital-config
data:
  DJANGO_ALLOWED_HOSTS: "api.dev.atc-capital.com,localhost"
  CORS_ALLOWED_ORIGINS: "https://app.dev.atc-capital.com"
  CSRF_TRUSTED_ORIGINS: "https://app.dev.atc-capital.com"
  DEPLOYMENT_ENVIRONMENT: "dev"
  APP_VERSION: "dev"