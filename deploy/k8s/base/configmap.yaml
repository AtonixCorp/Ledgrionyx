apiVersion: v1
kind: ConfigMap
metadata:
  name: atc-capital-config
data:
  DJANGO_DEBUG: "false"
  DJANGO_ALLOWED_HOSTS: "api.atc-capital.local,localhost"
  CORS_ALLOWED_ORIGINS: "https://app.atc-capital.local"
  CSRF_TRUSTED_ORIGINS: "https://app.atc-capital.local"
  DJANGO_SECURE_SSL_REDIRECT: "true"
  DJANGO_SESSION_COOKIE_SECURE: "true"
  DJANGO_CSRF_COOKIE_SECURE: "true"
  DEPLOYMENT_ENVIRONMENT: "base"
  APP_VERSION: "latest"